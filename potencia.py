base = float(input("Digite o valor da base: "))
expo = int(input("Digite o valor do expoente: "))

resultado = ()

for tabela in range (1 , expo):
    resultado = base * base
print("o resultado de", base , "elevado à", expo,"é", resultado)
